name: vitality
description: ManyBoost — Rewarded App Blocker Ecosystem
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  google_fonts: ^6.1.0
  intl: ^0.19.0
  flutter_animate: ^4.3.0
  google_mlkit_pose_detection: ^0.12.0
  camera: ^0.11.0+2
  google_mobile_ads: ^5.3.0
  shared_preferences: ^2.3.0
  share_plus: ^10.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
